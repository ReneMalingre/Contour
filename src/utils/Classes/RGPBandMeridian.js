class RGPBandMeridian {
  constructor(radius, eValue, angle, width, eyeAxis) {
    this.radius = radius;
    this.eValue = eValue;
    this.angle = angle;
    this.width = width;
    this.eyeAxis = eyeAxis;
  }
}
export default RGPBandMeridian;
